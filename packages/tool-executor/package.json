{
  "name": "@ai-company/tool-executor",
  "version": "0.1.0",
  "description": "Secure tool executor with schema validation for LLM tool calling",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-company/openapi-tools": "workspace:*",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  },
  "exports": {
    ".": "./dist/index.js",
    "./executor": "./dist/executor/index.js"
  }
}

